import { ScrollAlertDirective } from './scroll-alert.directive';

describe('ScrollAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAlertDirective();
    expect(directive).toBeTruthy();
  });
});
